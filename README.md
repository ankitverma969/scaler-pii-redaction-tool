# PII Redaction Tool

A DOCX PII detection and synthetic-redaction tool built for the Scaler AI Labs assignment.

Backend development:

```bash
cd backend
uvicorn app.main:app --reload
```

Frontend development:

```bash
cd frontend
npm install
npm run dev
```
